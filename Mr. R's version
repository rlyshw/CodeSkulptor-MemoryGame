import simplegui, random
def newgame(cards):
    lst = range(1,int(cards)+1)*2
    random.shuffle(lst)
    return lst
def texthandler(text):
    pass
def new():
    #try:
    cards = newgame(inp.get_text())
    print cards
    #except:
        #print "Please input a number"
print
def drawhandler(canvas):
    cards = newgame(texthandler)
#    for card in cards
frame = simplegui.create_frame("Memory", 500, 200)
inp = frame.add_input("# of cards", texthandler, 50)
frame.add_button("New Game", new)
